services:
  postgres:
    image: postgres:16-alpine
    container_name: signal-postgres
    environment:
      POSTGRES_USER: signal
      POSTGRES_PASSWORD: signal
      POSTGRES_DB: signal_harvester
    ports:
      - "5544:5432"
    volumes:
      - signal_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U signal -d signal_harvester"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build: .
    container_name: signal-harvester
    environment:
      DATABASE_URL: postgresql://signal:signal@postgres:5432/signal_harvester
      PORT: 3010
      MARKET_BACKFILL_ENABLED: ${MARKET_BACKFILL_ENABLED:-1}
      MARKET_BACKFILL_TARGET_WEIGHT_PER_MINUTE: ${MARKET_BACKFILL_TARGET_WEIGHT_PER_MINUTE:-3000}
      MARKET_BACKFILL_MAX_CONCURRENCY: ${MARKET_BACKFILL_MAX_CONCURRENCY:-16}
      MARKET_BACKFILL_MIN_INTERVAL_MS: ${MARKET_BACKFILL_MIN_INTERVAL_MS:-20}
      MARKET_BACKFILL_BATCH_SIZE: ${MARKET_BACKFILL_BATCH_SIZE:-1000}
    ports:
      - "3010:3010"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  signal_postgres_data:
